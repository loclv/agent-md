# Test Bold Removal

This line has **bold** text that should be removed.

This line has __underlined__ text that should also be removed.

## Multiple Bold Marks

Here is multiple **bold** **markers** in one line.

Here is multiple __underline__ __markers__ too.

### Bold Next to Code

Use `**code**` for inline code with asterisks.

Use `__code__` for inline code with underscores.

And here is normal bold that gets removed.
