```bash
 # Install cargo-generate if you haven't already
cargo install cargo-generate
```

should be:

```bash
# Install cargo-generate if you haven't already
cargo install cargo-generate
```

Remove the leading spaces before the comment - useless spaces.
